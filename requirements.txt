# Core dependencies
# Note: Install via system package manager (not pip):
#   sudo apt install ceph-common python3-rados
# Then create venv with: python3 -m venv --system-site-packages venv
# rados>=2.0.0  # Ceph RADOS Python bindings (system package only)

# Vector embeddings
sentence-transformers>=2.2.0
torch>=2.0.0

# Vector database
chromadb>=0.4.0

# LLM and NLP (for future extensions)
transformers>=4.30.0

# Data processing
numpy>=1.24.0
pandas>=2.0.0

# Configuration and schemas
pyyaml>=6.0
pydantic>=2.0.0

# CLI
click>=8.1.0
rich>=13.0.0  # Beautiful terminal output
tqdm>=4.65.0  # Progress bars

# Text processing
python-magic>=0.4.27
chardet>=5.1.0

# LLM Integration
ollama>=0.1.0  # Ollama Python client
openai>=1.0.0  # OpenAI API (optional)
httpx>=0.24.0  # HTTP client for API calls

# Utilities
python-dateutil>=2.8.0
